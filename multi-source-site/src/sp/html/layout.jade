- var isHome = path === '/';

doctype html
html(lang="ja")
  head(prefix="og: http://ogp.me/ns#")
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    if isHome
      title= title
    else
      title= `${title} - ${siteName}`
    meta(name="description", content= description)
    meta(name="format-detection", content="telephone=no")
    meta(property="og:title", content= title)
    meta(property="og:type", content= 'website')
    meta(property="og:image", content= origin + root + ogImage)
    meta(property="og:url", content= origin + typeRoot)
    meta(property="og:description", content= description)
    meta(property="og:locale", content="ja_JP")
    meta(property="og:site_name", content= siteName)
    meta(property="twitter:card", content="summary_large_image")
    link(rel="cannonical", href="#{root}#{path.substr(1)}")
    link(rel="stylesheet", href="#{typeRoot}css/style.css")

  body
    block content
    script(src="#{typeRoot}js/bundle.js")
